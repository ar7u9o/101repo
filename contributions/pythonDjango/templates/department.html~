{% extends "index.html" %}

{% block title %}

{{ dept.name }}

{% endblock %}


{% block list %}

<ul>

    <li>Manager:  <a class='department_name' href="../employee/{{ dept.manager.name }}">{{ dept.manager.name }}</a></li>
    <li><p /></li>


    <li>Subunits:</li>

{% for sub in dept.subunits %}

    <li><a class='department-name' href="../department/{{ sub.name }}">{{ sub.name }}</a></li>

{% endfor %}
    <li><p></li>
    {% if dept.employees %} <li>Employees:</li> {% endif %}

{% for sub in dept.employees %}

    <li><a class='department-name' href="../employee/{{ sub.name }}">{{ sub.name }}</a></li>

{% endfor %}

</ul>

{% endblock %}

